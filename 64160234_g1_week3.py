# -*- coding: utf-8 -*-
"""64160234_G1_Week3.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1uPEphPZYILIWyDJY_LrmYBobArClyUab

จงเขียนโปรแกรม เพื่อหาค่าที่โจทย์ต้องการและแสดงผลลัพธ์ออกทางหน้าจอ

1.	จงหาผลลัพธ์ที่เป็นผลหารจากการหาร 59 ด้วย 6

2.	จงหาผลลัพธ์ที่เป็นจำนวนเต็มจากการหาร 59 ด้วย 6

3.	จงหาผลลัพธ์ที่เป็นเศษเหลือที่เป็นจำนวนเต็มจากการหาร 59 ด้วย 6

4.	จงหาผลลัพธ์ที่เป็นผลหารจากการหาร -59 ด้วย 6

5.	จงหาผลลัพธ์ที่เป็นจำนวนเต็มจากการหาร -59 ด้วย 6

6.	จงหาผลลัพธ์ที่เป็นเศษเหลือที่เป็นทศนิยมจากการหาร -59 ด้วย 6

7.	จงตรวจสอบว่า 45 | 5 จริงหรือไม่ (ถ้าจริงแสดงข้อความ True ไม่จริงแสดงข้อความ False)

8.	จงตรวจสอบว่า 5 | 45 จริงหรือไม่ (ถ้าจริงแสดงข้อความ True ไม่จริงแสดงข้อความ False)

9.	จงรับค่าจำนวนเต็ม 3 ค่าจากผู้ใช้ผ่านหน้าจอ เก็บค่าไว้ในตัวแปร a, b, c จากนั้นทำการตรวจสอบว่า 

ถ้า a|b และ a|c จริง ให้ทำแสดงค่าความจริงของการตรวจสอบ a|(b+c)

กรณีอื่น แสดงค่าความจริงของการตรวจสอบ a|(b+c) 

10.	จงตรวจสอบว่า 787 เป็นจำนวนเฉพาะหรือไม่ ถ้าไม่เป็นจงบอกด้วยว่า 787 เกิดจากจำนวนเฉพาะใดคูณกันบ้าง

11.	จงตรวจสอบว่า 3851 เป็นจำนวนเฉพาะหรือไม่ ถ้าไม่เป็นจงบอกด้วยว่า มีเลขใดเป็นตัวประกอบ

12.	จงหาว่ามีจำนวนเฉพาะกี่จำนวนในช่วงตัวเลข 76 ถึง 102 ได้แก่เลขใดบ้าง 
ตัวอย่าง เช่น	ช่วงตัวเลข 10 – 31 มีจำนวนเฉพาะ	7 ตัว ได้แก่	11, 13, 17, 19, 23, 29, 31

13.  จงรับค่าจำนวนเต็ม n จากผู้ใช้ และตรวจสอบว่า n! mod 20 เป็นจำนวนเฉพาะหรือไม่ ถ้าไม่เป็นจงบอกด้วยว่า มีเลขใดเป็นตัวประกอบ
"""

# ชื่อ-สกุล แก่วมณี  มารสรี  กลุ่ม 1 รหัส 64160234 งานสัปดาห์ที่ 3
# ข้อ 1.จงหาผลลัพธ์ที่เป็นผลหารจากการหาร 59 ด้วย 6
print(59/6)

"""ผลลัพธ์ที่เป็นผลหารจากการหาร 59 ด้วย 6 = 9.833334"""

#2.จงหาผลลัพธ์ที่เป็นจำนวนเต็มจากการหาร 59 ด้วย 6
print(59//6)

"""ผลลัพธ์ที่เป็นจำนวนเต็มจากการหาร 59 ด้วย 6 = 9"""

#3.จงหาผลลัพธ์ที่เป็นเศษเหลือที่เป็นจำนวนเต็มจากการหาร 59 ด้วย 6
print(59%6)

"""ผลลัพธ์ที่เป็นเศษเหลือที่เป็นจำนวนเต็มจากการหาร 59 ด้วย 6 = 5"""

#4.จงหาผลลัพธ์ที่เป็นผลหารจากการหาร -59 ด้วย 6
print(-59/6)

"""ผลลัพธ์ที่เป็นผลหารจากการหาร -59 ด้วย 6 = -9.833334"""

#5.จงหาผลลัพธ์ที่เป็นจำนวนเต็มจากการหาร -59 ด้วย 6
print(-59//6)

"""ผลลัพธ์ที่เป็นจำนวนเต็มจากการหาร -59 ด้วย 6 = 10"""

#6.จงหาผลลัพธ์ที่เป็นเศษเหลือที่เป็นทศนิยมจากการหาร -59 ด้วย 6
print(-59%6)

"""ผลลัพธ์ที่เป็นเศษเหลือที่เป็นจำนวนเต็มจากการหาร -59 ด้วย 6 = 1"""

#7.จงตรวจสอบว่า 45 | 5 จริงหรือไม่ (ถ้าจริงแสดงข้อความ True ไม่จริงแสดงข้อความ False)
if 5%45 == 0:
  print('True')
else:
  print('False')
print(5%45)

"""45|5 False เพราะ 5 หาร 45 เหลือเศษ5 """

#8. จงตรวจสอบว่า 5 | 45 จริงหรือไม่ (ถ้าจริงแสดงข้อความ True ไม่จริงแสดงข้อความ False)
if 45%5 == 0:
  print('True')
else:
  print('False')
print(45%5)

"""5 | 45 is True เพระา 45 หาร 5 ลงตัว"""

#9. จงรับค่าจำนวนเต็ม 3 ค่าจากผู้ใช้ผ่านหน้าจอ เก็บค่าไว้ในตัวแปร a, b, c จากนั้นทำการตรวจสอบว่า  ถ้า a|b และ a|c จริง ให้ทำแสดงค่าความจริงของการตรวจสอบ a|(b+c) กรณีอื่น แสดงค่าความจริงของการตรวจสอบ a|(b+c)
a = int(input('Input number :'))
b = int(input('Input number :'))
c = int(input('Input number :'))
if (b%a == 0) and (c %a == 0) :
  print('{}|({}+{} ) = '.format(a,b,c),(b+c)%a)
  print('True')
else:
  print('{}|({}+{} )='.format(a,b,c),(b+c)%a)
  print('False')

"""เมื่อ b%a == 0 และ c %a == 0 จะค่าเท่ากับ (b+c)%a"""

#10.จงตรวจสอบว่า 787 เป็นจำนวนเฉพาะหรือไม่ ถ้าไม่เป็นจงบอกด้วยว่า 787 เกิดจากจำนวนเฉพาะใดคูณกันบ้าง
n = 787
if n > 1:
  for i in range(2, n):
    if (n % i) == 0:
      print(n, "is not a prime number")
      print('Because ',n,"=",i, "*", n // i)
      print('ตัวประกอบได้แก่ :')
      d = 2
      while  fac_> 1:
        if fac_ % d == 0:
          fac_ = fac_/d
          print(d)
        else:
          d = d+1
      break
  else:
    print(n, "is a prime number")
    
else:
  print(n, "is not a prime number")
  print('ตัวประกอบได้แก่ :')
  d = 2
  while  n> 1:
    if n % d == 0:
      n = n/d
      print(d)
    else:
      d = d+1

"""787 เป็นจำนวนเฉพาะ"""

#11. จงตรวจสอบว่า 3851 เป็นจำนวนเฉพาะหรือไม่ ถ้าไม่เป็นจงบอกด้วยว่า มีเลขใดเป็นตัวประกอบ
n = 3851
if n > 1:
  for i in range(2, n):
    if (n % i) == 0:
      print(n, "is not a prime number")
      print('ตัวประกอบได้แก่ :')
      d = 2
      while  n> 1:
        if n % d == 0:
          n = n/d
          print(d)
        else:
          d = d+1
          break
  else:
    print(n, "is a prime number")
    
else:
  print(n, "is not a prime number")
  print('ตัวประกอบได้แก่ :')
  d = 2
  while  n> 1:
    if n % d == 0:
      n = n/d
      print(d)
    else:
      d = d+1

"""3851 เป็นจำนวนเฉพาะ"""

#12. จงหาว่ามีจำนวนเฉพาะกี่จำนวนในช่วงตัวเลข 76 ถึง 102 ได้แก่เลขใดบ้าง ตัวอย่าง เช่น ช่วงตัวเลข 10 – 31 มีจำนวนเฉพาะ 7 ตัว ได้แก่ 11, 13, 17, 19, 23, 29, 31
lower = int(input("Enter a start number: "))
upper = int(input("Enter a end number: "))
n=0
for num in range(lower,upper + 1):
  if num > 1:
    for i in range(2,num):
      if (num % i) == 0:
        break
    else:
      print(num)
      n+=1
print("Number of prime number is ",n)

"""ช่วง 76-102 มีจำนวนเฉพาะ 5 ตัวได้แก่  79 83 85 97 101"""

#13. จงรับค่าจำนวนเต็ม n จากผู้ใช้ และตรวจสอบว่า n! mod 20 เป็นจำนวนเฉพาะหรือไม่ ถ้าไม่เป็นจงบอกด้วยว่า มีเลขใดเป็นตัวประกอบ
from math import factorial as fac
n = int(input('pls input number :'))
n = (fac(n))%20

if n > 1:
  for i in range(2, n):
    if (n % i) == 0:
      if n==0:
        print(n, "is not a prime number")
        print('0 ไม่มีตัวประกอบ')
        
      else:
        print(n, "is not a prime number")
        print('ตัวประกอบได้แก่ :')
        d = 2
        while  n> 1:
          if n % d == 0:
            n = n/d
            print(d)
          else:
            d = d+1
      break
  else:
    print(n, "is a prime number")  
else:
  print(n, "is not a prime number")
  if n == 0:
    print('ไม่สามารถหาตัวประกอบได้เนื่องจากมีค่าเป็น 0')
  else:
    d = 2
    while  n> 1:
      if n % d == 0:
        n = n/d
        print(d)
      else:
        d = d+1